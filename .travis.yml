language: ruby
rvm:
  - 2.3.1
services:
  - postgresql
before_script:
  - cp config/database.yml.sample config/database.yml
  - psql -c 'create database workshops_test;' -U postgres
addons:
  postgresql: '9.5.4'
